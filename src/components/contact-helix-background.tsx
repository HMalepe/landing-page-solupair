/** Soft brand wash behind the contact footer. CSS only — no WebGL loop. */
export function ContactHelixBackground() {
  return (
    <div className="contact-helix-canvas" aria-hidden>
      <div className="contact-helix-fallback" />
    </div>
  );
}
